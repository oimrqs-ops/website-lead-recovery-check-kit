# Example Handoff

- input: public page HTML or homepage URL
- task: check whether the main lead paths are visible and usable
- deliverable: Markdown report plus JSON summary
- check: re-run the CLI after CTA/contact fixes and compare failed items
- remaining risk: browser-only issues such as hidden overlays, blocked forms, cookie banners, or mobile layout regressions still need a visual QA pass
